# 還不快救主！v0.10.1 Render 線上對戰測試版

## 本版目的

將 v0.10.0 的房間、同步命令與鏡像視角整理成可直接部署到 Render Web Service 的工程包。
同一個 Node.js 服務同時提供遊戲網頁與 `/ws` WebSocket，不需要另外建立 Static Site。

## 新增內容

- 新增 `package.json`、`package-lock.json`、`render.yaml`。
- 遊戲首頁改為 `index.html`，Render 網址開啟後可直接進遊戲。
- 自動使用目前網頁所在主機的 `wss://.../ws`，正常情況不必手填伺服器網址。
- 客戶端每 20 秒送出心跳訊息，並監測伺服器回覆。
- 伺服器每 25 秒送出 WebSocket ping，清除失效連線。
- Render 部署或重啟送出 `SIGTERM` 時，伺服器會先通知房內玩家再關閉。
- 斷線後建立／加入按鈕會恢復可操作。
- 首頁文字改為「線上對戰」。

## 現有聯機功能

- 六位房間代碼。
- 玩家一建立、玩家二加入。
- 雙方取得同一亂數種子與 30Hz 戰鬥時鐘。
- 徵兵、棋子放置、收回兵欄、鏟子技能、投降會經伺服器排序後同步。
- 玩家一、玩家二手機都把自己顯示於下半場。
- 伺服器會以連線身分覆寫命令的 sideId，客戶端不能假冒另一方操作。

## 測試結果

- `server.js` 通過 `node --check`。
- HTML 內 23 段 JavaScript 全數通過 `node --check`。
- `/health` 回傳 HTTP 200 與房間／tickRate 資訊。
- 兩個 Chromium 手機視窗可建立與加入同一房間。
- 玩家一與玩家二身分、鏡像視角正確。
- 雙方各徵兵一次後，兩端資源與兵欄資料一致。
- 客戶端心跳收到伺服器 pong。
- 戰局快照一致，無 page error、無 console error。
- `SIGTERM` 可觸發正常關閉流程。

## Render 部署

這一包必須部署為 **Web Service**，不能只當 Static Site。

Dashboard 手動設定：

- Runtime：Node
- Build Command：`npm install`
- Start Command：`npm start`
- Health Check Path：`/health`

也可以在 Render 選擇 Blueprint，直接讀取根目錄的 `render.yaml`。

## 目前限制

本版已可讓兩台裝置進房並同步操作，但仍是「伺服器排序命令、兩端各自模擬」。
傷害、怪物、饅頭、經驗與勝負尚未全部移到權威伺服器，因此現階段適合真人連線測試，不是正式防作弊版本。
伺服器重啟、重新部署或休眠會中止現有房間，尚未加入斷線重連與狀態快照恢復。
