# 還不快救主！v0.10.3 房間連線修正版

## 修正內容

### 房間建立可靠性
- 原版 WebSocket 僅等待 6 秒，Render 免費服務休眠喚醒時很容易直接逾時。
- 新版先呼叫 `/health` 喚醒伺服器：互動建立房間時最長等待 60 秒。
- 健康檢查完成後，WebSocket 最長等待 45 秒。
- 建立／加入房間改為等待伺服器的 `room_created`／`room_joined` 正式回覆。
- 10 秒內沒有房間回覆會顯示「建立房間逾時」，並恢復兩個按鈕。
- 房間代號重複、找不到房間、房間已滿等錯誤會直接顯示在聯機視窗。

### 介面
- 移除聯機視窗右上角的叉號。
- 點擊視窗外的暗色背景即可關閉並取消尚未完成的連線。
- 首頁「線上對戰」新增狀態燈：黃燈檢查、綠燈在線、紅燈失敗。
- 綠燈時按鈕外框與陰影會發亮，不再像被停用的灰色按鈕。

### 伺服器健康資訊
- `/health` 新增 `version: 0.10.3` 與 `protocolVersion: 3`，方便確認 Render 是否部署到正確版本。

## 測試結果
- `server.js`：Node 語法檢查通過。
- HTML 內 23 段 JavaScript：全部通過 Node 語法檢查。
- `/health`：正確回傳版本、協定版本、房間數與 tick rate。
- Chromium 三個獨立頁面測試：
  - 玩家一建立 `ROOM3` 成功。
  - 玩家二加入相同房間成功。
  - 雙方列隊畫面顯示正確。
  - 第三個頁面建立重複房間時，視窗顯示「這個房間代號已被使用」，按鈕恢復可按。
  - 雙方按準備後同步進入 3 秒倒數。
  - 聯機視窗內不存在叉號。
  - 點擊暗色背景可正常關閉視窗。
  - 測試期間無 console 或 page error。

## 部署注意
本地測試已通過，但仍需把完整工程包重新推送 GitHub，等待 Render 完成新部署。舊的 Render 部署不會自動取得這些修正。
