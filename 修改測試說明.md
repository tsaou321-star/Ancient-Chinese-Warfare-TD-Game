# 還不快救主！v0.10.4 房間直連與存檔救援版

## 問題判定

Render 截圖中 `node server.js` 已成功啟動，服務也顯示 **Live**，所以伺服器程序本身並未死掉。房間視窗的 `Failed to fetch` 發生在瀏覽器端 `/health` 預先檢查；舊版將這個檢查當成建立房間的必要條件，因此健康檢查只要因來源網址、舊快取或網路問題失敗，WebSocket 根本不會開始連線。

存檔部分另有兩層問題：

1. `localStorage` 按網站來源隔離；本機檔案、GitHub Pages、不同 Render 網址不能互讀。
2. 舊救援流程有缺口：正式存檔完全不存在時沒有自動採用備份，而且舊遷移標記可能阻止再次搜尋舊版存檔。

## 聯機修正

- 正式伺服器位址集中設定為 `https://ancient-chinese-warfare-td-game.onrender.com`。
- 非正式 Render 頁面、GitHub Pages或下載版會直接連向上述 Render 服務。
- 建立／加入房間直接開啟 WebSocket；`/health` 只控制燈號，不再阻擋房間。
- `/health` 回應加入 CORS 與跨來源資源標頭。
- 取消不存在的 `sw.js` 註冊，並移除同來源舊 service worker，避免舊快取干擾。
- WebSocket 等待 60 秒；房間伺服器確認等待 12 秒。
- 伺服器 Logs 新增房間建立／加入紀錄。

## 存檔修正

- 正式 v10 存檔不存在或損壞時，依序尋找：
  1. v10 自動備份
  2. v9 自動備份
  3. v9～v2 舊版正式存檔
- 即使瀏覽器已有「先前找不到舊存檔」的遷移標記，也會重新搜尋。
- 找到後會重新建立 v10 正式存檔與備份。
- 跨網站來源的舊存檔仍須用遊戲內存檔碼搬家；這是瀏覽器安全邊界，無法由新版網頁直接讀取。

## 實際測試

- Node `server.js` 語法檢查通過。
- HTML 內 23 段 JavaScript 全數通過 `node --check`。
- 本機 `/health` 回傳 v0.10.4，且包含跨來源許可標頭。
- 在 Chromium 中刻意封鎖所有 `/health` 請求：
  - 玩家一仍可建立房間 `123`。
  - 玩家二仍可加入相同房間。
  - 雙方首頁聯機燈轉綠並顯示「房間已連線」。
  - 雙方準備後正常倒數並進入遊戲。
  - 玩家一與玩家二都保持自身棋盤在下方。
- 存檔測試：
  - 正式存檔重新載入可保留。
  - v10 備份可在正式存檔不存在時自動恢復。
  - v9 正式存檔在已有遷移標記時仍可重新搬移。
  - v9 備份可自動恢復。

測試結果：`game0104_test_results.json`。
