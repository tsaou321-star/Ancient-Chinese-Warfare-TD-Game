# 還不快救主！v0.10.7 修改與測試說明

## 1. 同源連線簡化

聯機網址不再經過自訂參數、固定 Render 網址或多層 URL 解析。

WebSocket 現在直接依目前頁面決定：

- `http://localhost:8787` → `ws://localhost:8787/ws`
- `http://127.0.0.1:8787` → `ws://127.0.0.1:8787/ws`
- `https://example.onrender.com` → `wss://example.onrender.com/ws`
- `file://...` → `ws://localhost:8787/ws`

健康檢查也使用相同 host。遊戲內提示改稱「房間伺服器」，避免本機測試時誤以為仍在呼叫 Render。

## 2. AI 帽子

單機模式每次開始新對局時，AI 會從目前帽子名冊中等機率隨機選一頂。AI 不再固定無帽。

帽子只在該局建立時抽一次，同一場戰鬥中不會自行變更。

## 3. AI 武將確認

原本的 `randomAiHeroLoadout()` 已符合需求：

- 每場新對局重新抽選。
- 從完整現有武將名冊抽四名。
- 四名互不重複。
- 所有武將等機率。
- 不複製或針對玩家編隊。

本版保留該流程並加入自動測試。

## 4. 測試項目

- Node `server.js` 語法檢查。
- HTML 所有內嵌 JavaScript 語法檢查。
- localhost 頁面自動產生 localhost WebSocket 網址。
- 直接開啟 file HTML 時只回退到 localhost，不含任何固定 Render 網址。
- 建立／加入房間與雙方準備倒數。
- 單機 AI 帽子必定為有效帽子。
- 多次新局的 AI 帽子可變動。
- 多次新局的 AI 武將編隊可變動，且每局四名不重複。
