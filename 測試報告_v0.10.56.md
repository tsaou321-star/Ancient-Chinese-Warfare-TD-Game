# v0.10.56 最終測試報告

## 自動化瀏覽器測試

以 Chromium 實際執行桌機 1280×900 與手機 390×844 兩種畫面，共完成 **84 項檢查（42×2），84 項全部通過**：

- 起始畫面、武將府、武器頁、天賦頁與戰鬥畫面正常載入。
- 所有武器、箭矢、白馬、骷髏、鏟子、包子、古錢與愛心圖片成功解碼。
- 左上包子、徵兵費用旁包子及大廳古錢圖示正常顯示。
- 新存檔未選武將時的空白提示正常；選擇武將後，武器總覽、篩選與長按詳情正常。
- 存檔匯出、玩家名稱修改及匯入還原往返成功。
- 開始遊戲、徵兵扣款、五張兵欄棋子、實際拖曳上場、單位詳情與兵營機率頁正常。
- 暫停、恢復與遊戲循環正常。
- 麻煩卡三選一、AI 自動選牌、玩家逾時代選、永久疊加倍率及額外怪物數量正常。
- 額外麻煩怪物不提供包子與武將經驗。
- 桌機與手機測試均無 JavaScript 執行例外。

## 聯機測試

- `/health` 回傳版本 0.10.56、協定 6、30 tick/s。
- 兩個實際 WebSocket 客戶端可建立房間、加入、雙方準備並同步開始。
- 雙方成功接收同步 tick 與相同合法麻煩卡指令。
- 偽造卡牌指令由伺服器拒絕；ping／pong 正常。

## 靜態與伺服器測試

- 24 個內嵌 JavaScript 區塊、合併腳本及 `server.js` 通過語法檢查。
- `npm run check` 通過。
- 原始 HTML 無重複 ID、無外部素材路徑、無舊「饅頭」顯示文字。
- 29 個內嵌圖片資料均為唯一內容，沒有重複塞入同一圖片。
- `/`、`/index.html`、`/game.html` 回傳 200 與正確 HTML 類型。
- 不存在路徑、`server.js`、`package.json` 等工程檔案回傳 404；非 GET 請求回傳 405。
- 最終 ZIP 完整性檢查通過。

## 測試界線

本次沒有發現阻斷遊戲的錯誤。自動測試無法取代所有實體手機、極長局數與每一種武將排列的人工遊玩，因此不能保證永遠零 bug；此版可作為後續開發的乾淨母版。
